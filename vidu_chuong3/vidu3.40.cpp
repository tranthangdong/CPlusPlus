#include <stdio.h>
#include<conio.h>
using namespace std;

int main()
{
    FILE *f;
    f = fopen("TEST.txt", "w");
    if (f!=NULL)
    {
        /* Các câu lệnh để thao tác với tập tin*/
        /* Đóng tập tin*/
    }
}

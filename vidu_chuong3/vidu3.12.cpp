#include <stdio.h>
#include<conio.h>
using namespace std;

int bp(int); // Khai báo hàm có đối kiểu và kiểu hàm là int
int rand100(); // Không đối, kiểu hàm (giá trị trả lại) là int
void alltrim(char[ ]);// Đối là xâu kí tự, không trả về giá trị
int cong(int, int); // Hai đối kiểu int, kiểu hàm là int

int main ()
{
  
}